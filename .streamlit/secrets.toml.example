# Copy to secrets.toml locally (gitignored) or paste into Streamlit Community Cloud → App settings → Secrets.
# SEC fair access: https://www.sec.gov/os/webmaster-faq#developers
#
# If you deploy to Streamlit Cloud, you must also set this in the environment before
# fetch_financials / fetch_market_data load, or bridge st.secrets → os.environ in dashboard.py.

# Replace with your real email for SEC fair-access policy.
ATLAS_SEC_USER_AGENT = "AtlasEquityResearch/1.0 (you@yourcompany.com)"
